package com.ura.casemgt.domain;

/**
 * Domain is where all business logic resides with classes and methods named using the ubiquitous language for the domain.
 * By controlling the domain through the API and putting all business logic into the domain the application becomes portable,
 * all technical bits can be extracted without losing any business logic.
 **/